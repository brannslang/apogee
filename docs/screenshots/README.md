Screenshots referenced by the top-level README, not yet captured. Run `streamlit run app/Home.py`
locally and save:

- `release-predictor.png` — the Release Predictor view (2_Release_Predictor.py) after running a
  prediction, showing the H/C probability, baseline comparison, and component-risk breakdown.
- `risk-map.png` — the Risk Map view (4_Risk_Map.py) bubble chart, showing the four optimization
  clusters (Critical Hotspot / Nuisance Zone / Low ROI / Stable Yielder).
- `industry-benchmark.png` — the Industry Benchmark view (6_Industry_Benchmark.py), ideally the
  trend-slope leaderboard or peer-percentile band section.

Delete this file once the three screenshots are in place.
